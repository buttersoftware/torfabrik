{% extends 'pspiessContentBundle::layout.html.twig' %}
{% set menu = 'project' %}

{% block title %}Bilder bearbeiten Admin{% endblock %}

{% block header %}
    {{ parent() }}
{% render controller('pspiessContentBundle:Navigation:header', {'header': 'Bilder ändern'}) %}

<div class="container">
    <div class="span12">
        <div class="row">
            {#{ form_label(edit_form) }#}
            {#{ form_errors(edit_form) }#}
            
            {{ form_widget(edit_form) }}
            {{ form(delete_form) }}
            <a class='btn btn-info' href="{{ path('pictures') }}">Zurück zur Übersicht</a>
        </div>
    </div>
</div>
{% endblock %}


