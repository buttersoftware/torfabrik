<footer class="full_width footer_block">
    <div class="container">
        <div class="row" data-animated="fadeInUp">
            <div id="navbar" class="navbar-collapse collapse">
                <ul class="nav navbar-nav">
                    <li><a class="btn btn-xs" href="{{ path('pspiess_blog_homepage') }}">Home</a></li>
                    <li><a class="btn btn-xs" href="{{ path('pspiess_blog_impressum') }}">{{ 'contact'|trans }}</a></li>
                    <li><a class="btn btn-xs" href="{{ path('pspiess_blog_partners') }}">Partnerseiten</a></li>
                    <li><a class="btn btn-xs" href="{{ path('pspiess_blog_advertise') }}">Werbemöglichkeiten</a></li>
                    <li><a class="btn btn-xs" href="{{ path('pspiess_blog_feedback') }}">Feedback</a></li>
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li><a class="btn btn-xs" href="{{ path('pspiess_blog_blog') }}">Links</a></li>
                        <li><a class="btn btn-xs" href="{{ path('pspiess_blog_maincategory') }}">Hauptkategorie</a></li>
                        <li><a class="btn btn-xs" href="{{ path('pspiess_blog_subcategory') }}">Unterkategorie</a></li>
                        {% endif %}
                        {% if not is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li><a class="btn btn-xs" href="{{ path('login') }}">login</a></li>
                        {% endif %}
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li><a class="btn btn-xs" href="{{ path('logout') }}">logout</a></li>
                        {% endif %}
                    <li><a href="{{ path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'de'})) }}">Deutsch</a></li>
                    <li><a href="{{  path(app.request.get('_route'), app.request.get('_route_params')|merge({'_locale': 'en'}))}}">English</a></li>
                </ul>
            </div>
        </div>
    </div>
</footer>