<link href="{{ asset('resources/css/blog/jquery-ui.min.css') }}" rel="stylesheet">
<link href="{{ asset('resources/css/blog/jquery-ui.custom.min.css') }}" rel="stylesheet">

<div id="dialog-message" class="hide">
    <form>
        <div class="form-group">
            <input type="text" class="form-control" id="link" placeholder="Link">
        </div>
        <div class="form-group">
            <input type="text" class="form-control" id="email" placeholder="Ihre E-Mail-Adresse (optional)">
        </div>
        <div class="form-group">
            <select name="problem" class="form-control form-control-custom">
                <option value="0">Der Link funktioniert nicht (mehr)</option>
                <option value="1">Der Link führt zu einer anderen Webseite</option>
                <option value="2">Die Location ist vorübergehend geschlossen</option>
                <option value="3">Die Location ist dauerhaft geschlossen</option>
                <option value="4">Die Adresse hat sich geändert (richtige bitte angeben)</option>
                <option value="5">Sonstige Probleme (bitte angeben)</option>
            </select>
        </div>
        <div class="form-group">
            <textarea name="report-message " id="report-message" placeholder="Bitte hier Ihre Nachricht eingeben"></textarea>
        </div>
    </form>
</div>

<script type="text/javascript">
    function showDialog(linkname) {
        $("#link").val(linkname);
        $("#report-message").val('');
        var dialog = $("#dialog-message").removeClass('hide').dialog({
            modal: true,
            title: "Ein Problem melden",
            title_html: true,
            width: 500,
            minHeight: 400,
            buttons: [
                {
                    text: "melden",
                    "class": "btn btn-primary btn-xs",
                    click: function () {
                        window.location.href = Routing.generate('pspiess_letsplay_customer_new'),
                                $(this).dialog("close");
                    }
                }
            ]
        });
        $("#add_reservation").attr("disabled", "disabled");
    }
</script>