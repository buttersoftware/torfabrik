{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Invoicepos creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('invoicepos') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
